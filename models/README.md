# Models Directory

Store trained model checkpoints here. The generator currently uses music21
for MIDI generation. Future versions will support PyTorch transformer models.

## Format
- `.pt` — PyTorch checkpoint
- `config.json` — Model configuration
- `vocab.json` — Token vocabulary
